import 'package:flutter/material.dart';
class JouerPage extends StatelessWidget {
  const JouerPage({super.key, required Map quizData});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text( "bienvenue")

    );

  }
}